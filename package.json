{
  "name": "parkinglots",
  "private": true,
  "scripts": {
    "postinstall": "npm ci --omit=dev --prefix backend || npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}